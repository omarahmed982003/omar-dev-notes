# ميزة الملاحظات ومتابعة التقدّم — للتنفيذ لاحقًا

> الحالة الحالية: مؤجّلة وغير مفعّلة داخل المشروع.
>
> هذا الملف يحفظ فكرة الميزة، الشكل الذي نُفّذ في النموذج الأول، والتعديلات المطلوبة قبل إعادتها.

## الهدف

إضافة نظام مذاكرة داخل الدوكس يسمح للمستخدم بـ:

1. كتابة ملاحظة بجوار أي عنوان داخلي في الدرس.
2. تعليم كل درس في السيدبار كمكتمل.
3. فتح صفحة واحدة تعرض كل الملاحظات وحالة التقدّم.
4. الاحتفاظ بالبيانات تلقائيًا دون قاعدة بيانات خارجية ودون تصدير واستيراد يدوي في الاستخدام اليومي.
5. دعم العربية والإنجليزية.

## تجربة الاستخدام المطلوبة

### الملاحظات داخل الدرس

- تظهر أيقونة ملاحظة واضحة بجوار عناوين H2 وH3 وH4.
- الضغط عليها يفتح نافذة منبثقة حديثة.
- النافذة تعرض:
  - اسم المسار أو الكارد.
  - اسم الدرس.
  - اسم العنوان الداخلي.
  - مربع كتابة الملاحظة.
  - أزرار الحفظ والحذف والإلغاء.
- العنوان الذي يحتوي على ملاحظة يحصل على حالة بصرية واضحة.
- يمكن تعديل الملاحظة أو حذفها لاحقًا.
- بعد الحفظ تظهر رسالة نجاح قصيرة.

### متابعة إنجاز الدروس

- تظهر أيقونة Checklist بجوار كل درس داخل السيدبار.
- الضغط عليها يغيّر حالة الدرس بين مكتمل وغير مكتمل.
- الدرس المكتمل يظهر بعلامة صح ولون مختلف، مع تخفيف النص دون التأثير على وضوحه.
- الحالة تظل محفوظة بعد Refresh وإغلاق المتصفح.

### صفحة الملاحظات

- يضاف رابط بأيقونة في الـNavbar باسم «ملاحظاتي وتقدّمي».
- يمكن وضع Badge صغير فوق الأيقونة يعرض عدد الملاحظات.
- الصفحة تعرض إحصائيات:
  - عدد الملاحظات.
  - عدد الدروس المكتملة.
- كل ملاحظة تعرض:
  - الكارد أو المسار.
  - السيكشن.
  - الدرس.
  - العنوان الداخلي.
  - نص الملاحظة.
  - تاريخ آخر تعديل.
- يوجد رابط يعيد المستخدم مباشرة إلى عنوان الملاحظة داخل الدرس.
- يوجد بحث داخل نص الملاحظات وأسماء الدروس والعناوين.
- توجد تصفية حسب المسار.
- يوجد حذف للملاحظة من الصفحة مع رسالة تأكيد.
- الصفحة متاحة بالعربية والإنجليزية:
  - `/notes/`
  - `/en/notes/`

## ما تم في النموذج الأول

النموذج الأول كان يعمل بالكامل في المتصفح باستخدام `localStorage`:

- إضافة وتعديل وحذف الملاحظات.
- تعليم الدروس كمكتملة.
- صفحة ملاحظات عربية وإنجليزية.
- البحث والتصفية.
- عدّاد في الـNavbar.
- تصدير البيانات إلى `omar-study-config.json`.
- استيراد الملف ودمجه مع البيانات الحالية.

تم اختبار النموذج ونجحت دورة الاستخدام كاملة، لكنه أُلغي لأن التخزين المحلي مرتبط بالدومين والمتصفح. تغيير رابط Cloudflare Quick Tunnel يجعل المتصفح يتعامل مع الموقع كمصدر جديد، ولذلك كان المستخدم سيحتاج إلى التصدير والاستيراد يدويًا.

## التعديل الأساسي عند إعادة الميزة

لا نعتمد على `localStorage` كمصدر البيانات الأساسي. نضيف API صغيرة على السيرفر تحفظ البيانات تلقائيًا في:

```text
data/study-config.json
```

يمكن استخدام `localStorage` فقط كـCache مؤقت وتحسين للسرعة، وليس كمصدر نهائي.

## شكل ملف الإعدادات المقترح

```json
{
  "version": 1,
  "notes": [
    {
      "id": "/programming-basics/03-tcp-udp-packets/#udp-vs-tcp",
      "locale": "ar",
      "track": "programming-basics",
      "trackLabel": "أساسيات البرمجة",
      "lessonPath": "/programming-basics/03-tcp-udp-packets/",
      "lessonTitle": "TCP وUDP والحزم",
      "headingId": "udp-vs-tcp",
      "headingText": "الفرق بين TCP وUDP",
      "body": "نص الملاحظة",
      "createdAt": "2026-09-22T10:00:00.000Z",
      "updatedAt": "2026-09-22T10:00:00.000Z"
    }
  ],
  "completedLessons": {
    "/programming-basics/03-tcp-udp-packets/": {
      "completed": true,
      "locale": "ar",
      "track": "programming-basics",
      "lessonTitle": "TCP وUDP والحزم",
      "updatedAt": "2026-09-22T10:00:00.000Z"
    }
  }
}
```

## الـAPI المقترحة

```text
GET    /api/study-config
PUT    /api/study-config
POST   /api/notes
PATCH  /api/notes/:id
DELETE /api/notes/:id
PATCH  /api/progress/:lessonId
```

يمكن البدء بنقطتين فقط لتقليل التعقيد:

- `GET /api/study-config` لقراءة الملف.
- `PUT /api/study-config` لحفظ النسخة الكاملة بطريقة آمنة.

بعد ذلك يمكن فصل العمليات إلى Endpoints مستقلة إذا احتجنا ذلك.

## الحماية المطلوبة

رابط Cloudflare Tunnel قد يكون عامًا، لذلك لا يجب ترك الكتابة في ملف JSON متاحة لأي شخص.

التنفيذ القادم يجب أن يحتوي على:

- كلمة مرور واحدة للمالك أو جلسة دخول بسيطة.
- Cookie آمنة بعد تسجيل الدخول.
- منع الكتابة بدون جلسة صحيحة.
- التحقق من حجم الملاحظة ونوع جميع الحقول.
- كتابة الملف بطريقة Atomic: الكتابة أولًا في ملف مؤقت ثم استبدال الملف الأصلي.
- الاحتفاظ بنسخة احتياطية قبل الاستبدال.
- Rate limiting بسيط على عمليات الحفظ.
- عدم وضع كلمة المرور أو Secret داخل كود الواجهة.

## سلوك الحفظ المقترح

1. المستخدم يكتب الملاحظة.
2. الواجهة تعرض حالة «جارٍ الحفظ».
3. يرسل الطلب إلى الـAPI.
4. السيرفر يتحقق من البيانات والصلاحية.
5. السيرفر يحدّث ملف JSON بطريقة Atomic.
6. الواجهة تعرض «تم الحفظ».
7. عند فشل الاتصال تبقى نسخة مؤقتة محليًا ويعاد إرسالها عند عودة الاتصال.

يفضل استخدام Debounce عند تعديل الملاحظة، مع زر حفظ واضح كخيار أساسي.

## الملفات المتوقعة عند التنفيذ

```text
src/components/StudyTools.astro
src/components/NotesDashboard.astro
src/components/DocsFooter.astro
src/scripts/study-store.ts
src/pages/api/study-config.ts
src/content/docs/notes.mdx
src/content/docs/en/notes.mdx
data/study-config.json
```

وسيحتاج `DocsHeader.astro` إلى رابط صفحة الملاحظات، كما يحتاج `astro.config.mjs` إلى ربط المكوّن العام واختيار Output/Adapter يسمح بتشغيل الـAPI على السيرفر.

## قرارات يجب حسمها قبل التنفيذ

1. هل الموقع للاستخدام الشخصي فقط أم سيكون لكل مستخدم حساب؟
2. هل سيظل التشغيل على نفس الكمبيوتر عبر Cloudflare Tunnel؟
3. هل نستخدم كلمة مرور واحدة أم نظام تسجيل دخول؟
4. هل نكتفي بملف JSON أم نستخدم SQLite؟
5. هل حالة إنجاز النسخة العربية والإنجليزية مشتركة أم منفصلة؟
6. هل يمكن إضافة أكثر من ملاحظة لنفس العنوان أم ملاحظة واحدة فقط؟

## اقتراح التنفيذ

للاستخدام الشخصي الحالي:

- ملف JSON واحد.
- API صغيرة داخل Astro/Node.
- كلمة مرور واحدة وجلسة آمنة.
- ملاحظة واحدة لكل عنوان قابلة للتعديل.
- مشاركة حالة الإنجاز بين العربية والإنجليزية باستخدام معرف موحّد للدرس.
- نسخ احتياطي تلقائي لملف الإعدادات.

إذا أصبح الموقع متعدد المستخدمين لاحقًا، ننقل نفس نموذج البيانات إلى Supabase أو PostgreSQL مع حساب مستقل لكل مستخدم.

## شروط قبول الميزة

- لا يحتاج المستخدم إلى تصدير أو استيراد أي ملف أثناء الاستخدام الطبيعي.
- تغيير رابط Cloudflare لا يفقد الملاحظات.
- الملاحظة وحالة الدرس تظهران بعد Refresh.
- لا يستطيع زائر غير مسجل تعديل الملف.
- العربية والإنجليزية تعملان دون تكرار أو تعارض في البيانات.
- فشل الحفظ يظهر بوضوح ولا يوهم المستخدم بأن البيانات حُفظت.
- يمكن أخذ نسخة احتياطية واستعادتها عند الحاجة.
- تعمل الواجهة على الموبايل والكمبيوتر وتدعم RTL وLTR.
